namespace :radiant do
  namespace :extensions do
    namespace :kramdown_filter do
      task :migrate do
        puts("The kramdown_filter doesn't have any migrate tasks to run.")
      end
      task :update do
        puts("The kramdown_filter doesn't have any static assets to copy.")
      end
    end
  end
end
